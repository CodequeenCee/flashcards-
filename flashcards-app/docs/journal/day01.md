# What I built
I fixed the delete bug that cleared all the stored flashcards that were in the local storage.

I changed it to delete just the card that was been clicked at any instant

Wrote a real README and includued a screenshot of the app

Used git from the terminal for every change i made to any files  

# What Broke
The delete butoon to remove cards that are not needed, it deleted everything instead of the desired card
